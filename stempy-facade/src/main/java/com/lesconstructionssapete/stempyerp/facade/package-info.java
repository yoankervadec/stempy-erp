/**
 * This package contains the facade classes for the Stempy ERP system. The
 * facade pattern is used to provide a simplified interface to a complex
 * subsystem, making it easier for clients to interact with the system without
 * needing to understand its internal workings.
 * 
 * Orchestrates multiple services, handles cross-cutting concerns, transaction
 * boundaries, context-aware error handling, and ensures a clean separation of
 * concerns between the presentation layer and the underlying business logic and
 * data access layers.
 */
package com.lesconstructionssapete.stempyerp.facade;
